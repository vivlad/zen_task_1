import React from 'react';
import Footer from '../Footer';
import Header from '../Header';

const About = () => (
    <div>
        <Header />
        <div>That is info about our project</div>
        <Footer />
    </div>
);

export default About;